*.pyc

**/__pycache__/**

.venv/
*.egg-info/
embedded/build/
embedded/sdkconfig

.claude/
.vercel

# Secrets / local env — never commit (keys live here, not in tracked files)
.env
.env.*
!.env.example
